# TypeScript scripting

TypeScript uses the **same Deno runtime** as JavaScript. Types are stripped at run time — no separate build step.

| Page | Description |
|------|-------------|
| [Getting started](getting-started.md) | Enable TypeScript on the Scripts tab |
| [Typed examples](typed-examples.md) | Annotations for requests and responses |

Shared topics: [Execution order](../execution-order.md), [Debugging](../debugging.md), [External packages](../javascript/external-packages.md) (same `pm.require` rules).

API reference: [JavaScript API](../../../scripting/javascript-api.md) (TypeScript uses the same `pm.*` surface).
